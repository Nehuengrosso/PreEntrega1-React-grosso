import CartWidget from "./CartWidget";

const Barra = ({greeting}) => {
    return (
        <ul class="nav">
            <li class="nav-item">
                <a class="nav-link active" href="#">Home</a>
            </li>
        </ul>
    )
}

export default Barra;